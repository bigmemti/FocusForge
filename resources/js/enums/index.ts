export enum TaskStatus {
    TODO = 0,
    IN_PROGRESS = 1,
    TESTING = 2,
    DONE = 3,
    BACKLOG = 4,
    BLOCKED = 5,
    READY_FOR_DEPLOYMENT = 6,
    REJECTED = 7,
}

export enum TaskPriority {
    HIGHEST = 0,
    HIGH = 1,
    MEDIUM = 2,
    LOW = 3,
    LOWEST = 4,
}

